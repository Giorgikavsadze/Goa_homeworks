const user=prompt("Enter traveled distance")
const user1=prompt("enter used fuel")

const distance=parseFloat(user )
const fuel=parseFloat(user1 )
const kmPerLiter=distance /fuel
console.log(kmPerLiter)